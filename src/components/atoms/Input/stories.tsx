import { Meta, StoryObj } from '@storybook/react'
import Input from '.'

export default {
    title: 'Components/Atoms/Input',
    component: Input,

    tags: ['autodocs'],
} as Meta

type Story = StoryObj

export const Default: Story = {}
