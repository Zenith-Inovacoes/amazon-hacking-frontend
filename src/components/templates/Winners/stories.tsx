import { Meta, StoryObj } from '@storybook/react'
import Winners from '.'

export default {
  title: 'Components/Templates/Winners',
  component: Winners,

  tags: ['autodocs'],
} as Meta

type Story = StoryObj

export const Default: Story = {}
