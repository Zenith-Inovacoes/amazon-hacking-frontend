import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Not Authorized | Amazon Hacking",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
